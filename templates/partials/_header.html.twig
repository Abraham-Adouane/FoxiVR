
<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <a class="navbar-brand brandname" href="{{path('app_home')}}">
  <img src="/ressources/logo/Elegant_Wolf_logo-removebg-preview.png" width="50px" alt="logo">
  FoxiVR</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      {# start temporaire #}
      <li class="nav-item">
        <a class="nav-link " href="{{path('app_product')}}">Nos produits</a>
      </li>
      {# end temporaire #}
      {% include 'components/_dropdown.html.twig' %}
    </ul>
    <ul class="navbar-nav ms-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{path('cart_index')}}">
          <i class="fas fa-shopping-cart"></i>
        </a>
      </li>
    {% if (app.user) %}
    {# {{dump(app.user)}} #}
      {% if ('ROLE_ADMIN' in app.user.roles) %}
      <li class="nav-item"><a class="nav-link" href="{{ path('app_admin') }}"><i class="fa-solid fa-gear"></i> Administration</a></li>
      {% endif %}

      <li class="nav-item"><a class="nav-link" href="{{ path('app_profile_index') }}"><i class="fa-solid fa-user"></i> Mon compte</a></li>
      <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}"><i class="fa-sharp fa-solid fa-right-from-bracket"></i> Se deconnecter</a></li>
    {% else %}
      <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}"><i class="fa-sharp fa-solid fa-right-to-bracket"></i> Connexion</a></li>
      <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
    {% endif %}

    </ul>
  </div>
</nav>